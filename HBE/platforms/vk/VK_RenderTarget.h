#pragma once

#include "vulkan/vulkan.h"
#include "core/graphics/RenderTarget.h"

namespace HBE {
	class VK_RenderTarget : public RenderTarget {
		/*VkViewport viewport{};
	public:
		VK_RenderTarget(uint32_t width, uint32_t height);
		VkViewport& getViewport();
		void begin(VkCommandBuffer command_buffer);
*/
	};
}

